pipeline {
    agent {
        label 'master'
    }

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
                sh 'echo "Hello Jenkins!"'
                sh 'date'
            }
        }
    }
}