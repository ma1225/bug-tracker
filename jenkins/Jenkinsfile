pipeline {
    agent any

    stages {
        stage('Unit Tests - Backend') {
            steps {
                dir('bugtracker-backend') {
                    // clear any global DOCKER_HOST that points to "docker:2376"
                    withEnv(['DOCKER_HOST=']) {
                        sh '''
                          docker pull golang:1.21 || true
                          docker run --rm -v "$PWD":/go/src/app -w /go/src/app golang:1.21 sh -c "go test -v ./..."
                        '''
                    }
                }
            }
        }
    }
}